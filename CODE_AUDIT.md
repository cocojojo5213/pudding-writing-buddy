# Code Audit

This file records the current hardening pass after reading the project source with line numbers. The main objective was to find defects that could lose manuscript data, hide persistence failures, or make the writing assistant unreliable during longer novel workflows.

## Current Pass Summary

- `lib.js` lines 18 and 140-168: `DEFAULT_PROJECT` is now deep-frozen, `normalizeProject()` preserves an existing `updatedAt`, rounds legacy decimal fractional `targetWords` values to whole counts, falls back to the default for malformed/null/non-decimal target counts, and the project schema carries a `versionToken` used for write-conflict detection.
- `lib.js` normalization helpers: legacy imported characters/hooks/outline/timeline/resources/arcs/chapters that lack ids now receive deterministic content-based fallback ids instead of fresh random ids on every read, and duplicate, control-character, or non-string/non-numeric legacy collection ids are repaired with stable replacement ids so frontend id-based editing cannot target the wrong record. Missing chapter `createdAt` no longer gets a new timestamp during pure normalization, explicit empty chapter `body` values are no longer overwritten by legacy `text` fields, malformed object/array/boolean legacy text fields no longer stringify into `[object Object]` or comma-joined fake prose, malformed chapter `body` shapes can still fall back to a valid legacy `text` field, and matching user entries no longer inherit default-story character knowledge, hook notes, resources, or arc details just because they share a demo name or text.
- `lib.js` exported offline helpers now normalize sparse/null project objects and treat non-object payloads as empty payloads before reading nested fields. Direct calls to `buildPrompt()`, `offlineAssist()`, `offlinePlan()`, `offlineDraft()`, `offlineAudit()`, `offlineBrainstorm()`, and settlement helpers no longer crash on missing project collections, null payloads, or non-string draft/style/revision text; assistant prompt payload slots such as `instruction`, `plan`, `text`, `audit`, `chapterId`, and `chapterNumber` now drop object/array values instead of leaking fake `[object Object]` instructions into model prompts; direct text helper calls now keep string/finite-number compatibility while rejecting object/array/boolean text and audit values so malformed inputs do not become fake prose, fake plan keywords, fake audit triggers, or `[object Object]` report fields; empty settlement input still returns the intended "without body text" validation error.
- `lib.js` chapter reference, settlement report, and export helpers: numeric-looking chapter ids are now resolved as ids before being treated as chapter numbers, and empty, fractional, object, array, or boolean chapter refs fall back to the next chapter instead of producing `第1.5章`, matching stringified fake ids, or wrapping prior context to the last chapter. Chapter summaries, scalar metadata lines, and generated settlement report fields now use single-line sanitation, so summary, genre, logline, title, timeline, or ledger update text cannot inject fake Markdown sections while manuscript body text remains intact.
- `lib.js` lines 566-657: chapter settlement now inserts a chapter when the client sends an unsaved chapter, merges partial incoming chapter payloads with the saved chapter before extracting summary/hooks/character knowledge, preserves existing title/body/plan/audit/status/created timestamp fields when direct incoming settlement fields are blank, updates truth ledgers, records character knowledge from evidence sentences instead of only generic text, appends character/hook/resource ledger notes instead of overwriting older state, refreshes the prior auto-generated settlement timeline event for the same chapter instead of accumulating stale summaries, keeps same-title chapter settlement timeline entries separate by `chapterId`, and preserves timeline entries that were manually revised after settlement.
- `server.js` lines 63-157: API method guards now return `405` with an `Allow` header for known routes. Invalid JSON, non-object JSON request bodies, unknown assist tasks, malformed non-string assist task shapes, and malformed explicit assist `payload`/`modelConfig` shapes are covered by regression tests.
- `server.js` static serving: malformed percent-encoded paths now return a plain `400 Bad request`; static files only allow `GET`/`HEAD`; encoded slash traversal attempts are covered by regression tests; `HEAD` returns headers without a body; URL parsing uses a fixed localhost base instead of trusting the request `Host` header; and successful static responses read the file before sending `200` headers, so both `GET` and `HEAD` read failures return a clean `404` instead of reporting inconsistent success.
- `server.js` request parsing: JSON bodies are now assembled as buffers before UTF-8 decoding, so multibyte characters split across network chunks are not corrupted. Oversized request bodies are rejected before retaining the chunk that crosses the size limit. Top-level JSON must be an object.
- `server.js` startup config: `PORT` and `STALE_TEMP_MS` are parsed as bounded integer settings with safe defaults, so malformed, fractional, zero, or out-of-range environment values no longer crash startup or distort stale temp cleanup.
- `server.js` lines 189-287: corrupt `project.json` is backed up before resetting to a default project, and corrupt-backup filenames include a random suffix to avoid overwrite collisions. Missing files still bootstrap cleanly. Saves are atomic, return the exact project written to disk, tolerate stale-temp cleanup races, and rotate `versionToken` on every write.
- `server.js` lines 204-256: write routes now serialize `versionToken` checks and disk writes through one in-process queue. Concurrent same-version saves cannot both pass the check; stale project, settle, and reset writes return `409` instead of overwriting newer work. Blind writes without any version marker are also rejected when a project already exists, and malformed version metadata such as array/object tokens or timestamps is rejected before it can be coerced into a matching string. `/api/project` now requires a complete project payload with scalar project fields, core ledger arrays, object-shaped collection items, nonblank/unpadded/control-character-free unique ids for collection items, required string fields for each ledger item, valid project language, valid hook statuses, and decimal whole-number target word counts inside the same 300-20000 range used by core normalization, so bare tokens, sparse project objects, malformed fields, invalid enum values, unsafe ids, duplicate ids, fractional/exponent/hex/out-of-range targets, or malformed collections cannot normalize into defaults/bounds and overwrite the manuscript. Routes that accept an optional explicit project now apply the same complete-shape checks instead of silently falling back to disk/default state. `/api/settle` now uses the current disk project as its write base after the version check, so stale client project payloads cannot overwrite saved title, notes, or chapter metadata during settlement. Malformed explicit settlement chapter payloads are rejected before any disk write, malformed chapter ids no longer stringify arrays/objects/booleans into saved chapter ids, and partial settlement merging now ignores object/array/boolean text slots instead of turning malformed incoming `body` values into `[object Object]`; a valid legacy `text` field can still recover the chapter body.
- `server.js` model-call section: model responses are parsed defensively. A successful non-JSON model response now reports `Model response was not valid JSON` instead of an opaque server exception, successful `null` JSON reports missing text content instead of an internal type error, common OpenAI-compatible output shapes are extracted consistently, oversized model response bodies are rejected before being fully buffered, model Base URLs are validated with URL parsing, usernames/passwords in model Base URLs are rejected, full `/chat/completions` endpoints are not double-appended, malformed object/array model-config text fields no longer stringify into usable `baseUrl`, `apiKey`, or `model` values and therefore cannot proxy `Bearer [object Object]` requests, malformed array/object/boolean model numeric fields no longer stringify into usable `temperature` or `maxTokens` values, temperature/token limits are clamped server-side before proxying, direct or DNS-resolved private/link-local/multicast/metadata targets are rejected by default while local loopback gateways remain allowed, IPv4 targets hidden behind non-standard numeric IPv4 forms, IPv4-mapped IPv6, IPv4-compatible IPv6, NAT64 well-known IPv6, or 6to4 IPv6 transition addresses are also classified before proxying, model HTTP(S) requests use the checked DNS result for the actual connection, and model proxy redirects are no longer followed automatically.
- `server.js` model-call and static-serving edges: failed JSON model responses now preserve upstream `error` or `message` text instead of collapsing everything into the HTTP status line, including primitive, `null`, or array-shaped JSON error bodies; `2001:0000::/32` Teredo-style transition addresses are also checked for hidden private or loopback IPv4 endpoints; and static file serving resolves `realpath` before reading so a symbolically linked file inside `public/` cannot escape to content outside the static root.
- `server.js` export/snapshot paths: snapshot filenames include a unique suffix, export filenames strip path/control characters, and Markdown headings plus ledger/list fields collapse line breaks so bad titles or state fields cannot split document structure.
- `public/app.js` lines 336-572: autosave now has `flushSave()`, save queuing, revision tracking, dirty marking for both user edits and generated results, and `versionToken` submission. Delayed autosave responses no longer overwrite newer local edits, `flushSave()` retries unsaved dirty revisions even after a previous autosave failure, stale browser sessions surface a conflict message, user-triggered metric refresh no longer hides save/API failures and now renders the server-returned disk metrics instead of stale local counts, neutral actions such as snapshot/model-save do not paint the status green while the main project is still dirty, invalid target-word values including fractional/exponent/hex-formatted counts are shown locally without sending doomed autosaves, background autosave failures no longer overwrite export/snapshot rescue output, and closing/reloading the tab warns while a save is pending or unsaved revisions remain.
- `public/app.js` control binding: project fields and ledger collection editors now listen to the correct value-change event for each control type, so `select` fields such as language and hook status reliably mark the project dirty and enter the autosave/export path across browsers.
- `public/app.js` chapter editor: typing into an empty chapter editor now creates the backing chapter without re-rendering the editor over the first user edit, so a first line entered before pressing Add is preserved through save. The generated default chapter title is also synchronized back into the title input during implicit chapter creation, preventing a later plan/body/audit/summary edit from clearing the title before save.
- `public/app.js` action binding and locking: long-running actions now use a logical action lock and disable controls while active, preventing concurrent plan/draft/audit/settle flows from racing each other. Add-character/hook/outline/timeline/resource/arc/chapter writes, collection deletes, form edits, and chapter switching also enter the same guard, so script-triggered events cannot mutate the project or retarget generated output while a guarded action is already running. Generated-result saves skip a final DOM re-collection pass, so disabled form values changed by script while a model request is pending cannot pollute the saved project.
- `public/app.js` startup/model config, settlement display, reset, and export flow: project operations stay disabled until `/api/project` loads, export/snapshot remain usable as local rescue paths during conflicts, pending autosave timers are canceled before snapshot/Markdown rescue actions so delayed save failures cannot overwrite rescue output, in-flight autosave failures are handled in the save status instead of replacing rescue output, confirmed Reset ignores prior in-flight autosave failures while still sending version metadata for server-side conflict protection, model config writes and reads tolerate unavailable browser storage, malformed stored model config is cleared or field-level defaulted instead of breaking startup, inspect-style actions no longer auto-create blank chapters, API-provided settlement report fields are collapsed to single lines before display, malformed settlement report objects/arrays/booleans fall back instead of showing `[object Object]` or fake text, malformed or blank assist output is rejected before mutating chapters or style fields, malformed export/snapshot API filename or Markdown fields are rejected before showing fake paths/content or triggering downloads, Settle reselects the actual chapter returned by the server so repaired chapter ids cannot leave the editor blank, and Markdown downloads click a temporary mounted link before cleanup while object URL revocation is deferred to avoid canceling downloads in stricter browsers.
- `public/styles.css` mobile layout: small screens now show the workspace before the configuration sidebar, keep all five main tabs visible, and preserve access to project/model controls further down the page.
- `test/config.test.js`, `test/core.test.js`, `test/server.test.js`, and `test/frontend.test.js`: coverage now includes bounded decimal-only server environment parsing, timestamp preservation, immutable defaults, deterministic legacy ids, stable duplicate/unsafe/non-string legacy id repair, malformed legacy text field fallback, malformed chapter body fallback to legacy text, fractional and malformed legacy target-word normalization, legacy chapter text migration without reviving explicit empty bodies, default-story normalization leak prevention, assistant payload text sanitization for prompts/offline helpers, first-chapter context behavior, numeric-looking chapter id resolution, fractional/empty/malformed chapter ref fallback, exported offline helper tolerance for sparse projects/null payloads/non-string text, direct exported text-helper rejection of object/array/boolean text and audit values, malformed settlement-report field fallback, markdown export ledger, scalar metadata, chapter-summary, and settlement-report line safety, empty/partial/blank-body settlement, blank incoming settlement metadata preservation, stale summary refresh, settlement timeline refresh, same-title settlement timeline separation, manual timeline preservation, append-only truth ledger updates, HTTP invalid/non-object JSON, split multibyte JSON bodies, oversized request body rejection without saved-project mutation, API and static method guards, `HEAD` static handling, Host-header-safe static parsing, static traversal rejection, symlink escape rejection through `realpath`, unreadable static `GET`/`HEAD` fallback behavior, unknown task handling, malformed assist task/payload/model config rejection, malformed model-config text and numeric fields falling back without unsafe string coercion, malformed incoming settlement id rejection before saved body reuse, malformed incoming settlement body fallback to legacy `text`, unsafe/private model target URL rejection including username/password-bearing model URLs, unsafe DNS-resolved loopback hosts, non-standard numeric IPv4 forms, IPv4-mapped IPv6, IPv4-compatible IPv6, NAT64 well-known IPv6, Teredo transition checks, and 6to4 IPv6 transition addresses, model redirect suppression, oversized model response rejection, model error detail preservation for object/string/number/boolean/null/array JSON bodies, blind write rejection, malformed project version metadata rejection, malformed static paths, project-save payload shape rejection, optional-project payload shape rejection, sparse optional-project payload rejection, malformed scalar project field rejection including fractional/exponent/hex-formatted or out-of-range target word counts, invalid project enum rejection, malformed project collection item rejection, missing or malformed collection item field rejection, blank/padded/unsafe/duplicate collection id rejection, invalid hook status rejection, malformed settlement chapter payload rejection, unique corrupt backups, non-JSON and multi-shape model responses, unique snapshots, sanitized export filenames, mounted Markdown export download cleanup, malformed assist API output rejection before chapter mutation, blank assist output rejection before clearing generated fields, malformed export/snapshot API response field rejection before fake downloads or fake paths, target-word client validation without autosave requests, refreshed server metrics rendering, settlement editor reselection when returned chapter ids change, frontend malformed settlement output fallback, implicit chapter title preservation, collection field edits across dynamic row cards, timeline source-link clearing after manual timeline edits, snapshot/Markdown rescue output protection from pending and in-flight autosave failures, stale write rejection, concurrent same-token save serialization, stale atomic-save temp cleanup, autosave failure retry before long-running actions, confirmed Reset after failed in-flight autosave, Add action rejection while another guarded action is running, chapter-switch rejection while another guarded action is running, locked form edit/delete rejection while another guarded action is running, locked collection input rejection while another guarded action is running, and the empty-editor first chapter save regression.

## Residual Risks

- The settlement extractor is heuristic. It is useful for offline state capture, but it does not fully understand entity aliases, implied character knowledge, or resource quantities.
- Offline drafting remains deterministic and template-shaped. It is serviceable as a no-model fallback, but model-backed drafting is still the intended higher-quality path.
- Write-conflict protection is in-process. It protects the local single-process server used by this app; if the app is later deployed as multiple Node processes writing the same file, the same version check would need an inter-process lock or database transaction.
- Frontend behavior still has only focused DOM-stub regression coverage plus browser screenshots/manual runtime probes, not a full browser automation suite.

## Verification

Latest verification commands:

```bash
npm test
node --check lib.js
node --check server-config.js
node --check server.js
node --check public/app.js
node --check test/config.test.js
node --check test/core.test.js
node --check test/server.test.js
node --check test/frontend.test.js
```

Result: 100 tests passing, including config parsing tests, core library tests, focused frontend DOM-stub regression tests, and real HTTP API subprocess tests. Runtime checks on `http://127.0.0.1:5179` previously verified health, static `POST`/`HEAD` behavior, Host-header-safe static parsing, malformed static path handling, non-object JSON rejection, empty-settle rejection, unique snapshots, sanitized export filenames, clean project restoration, and desktop/mobile screenshots.
